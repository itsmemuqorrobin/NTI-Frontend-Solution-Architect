type ImageType = string;

const BaseImageURL: ImageType = `/images/`;

export const IllustrationImageURL = `${BaseImageURL}illustrations/`;

export const ComparisonImageURL = `${IllustrationImageURL}comparations`;
